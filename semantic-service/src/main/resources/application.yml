server:
  port: 8084
spring:
  application:
    name: semantic-service
  data:
    redis:
      host: localhost
      port: 6379
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:dummy}
      chat:
        options:
          model: gpt-4o-mini
platform:
  memory:
    type: local
  vector-store:
    milvus:
      host: localhost
      port: 19530
dubbo:
  application:
    name: semantic-service
  protocol:
    name: dubbo
    port: 20881
  registry:
    address: N/A
